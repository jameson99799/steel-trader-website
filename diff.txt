diff --git a/src/components/SiteHeader.vue b/src/components/SiteHeader.vue
index b8f9fbd..282d193 100644
--- a/src/components/SiteHeader.vue
+++ b/src/components/SiteHeader.vue
@@ -1002,10 +1002,14 @@ onUnmounted(() => {
   .header-main {
     height: 92px !important;
     contain: layout size;
+      position: relative;
+      z-index: 10;
   }
   .header-top {
     height: 44px !important;
     contain: layout size;
+      position: relative;
+      z-index: 20;
   }
 }
 </style>
diff --git a/src/views/NewsDetail.vue b/src/views/NewsDetail.vue
index e2d6fef..72b3e0b 100644
--- a/src/views/NewsDetail.vue
+++ b/src/views/NewsDetail.vue
@@ -299,22 +299,6 @@ function handleMailtoClick(href, event) {
 }
 
 function handleBodyClick(e) {
-  const anchor = e.target.closest('a')
-  if (anchor) {
-    const href = anchor.getAttribute('href')
-    if (href && href.startsWith('#')) {
-      e.preventDefault()
-      const id = href.slice(1)
-      const el = document.getElementById(id) || document.getElementById(decodeURIComponent(id))
-      if (el) {
-        const top = el.getBoundingClientRect().top + window.scrollY - 100 // 100px fixed header offset
-        window.scrollTo({ top, behavior: 'smooth' })
-        history.pushState(null, null, href)
-      }
-      return
-    }
-  }
-
   const videoContainer = e.target.closest('.video-container')
   if (videoContainer) {
     e.preventDefault()
